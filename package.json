{
  "name": "@vesa-gg/core",
  "description": "Shared VESA domain logic and Nhost data access — consumed by scrim-bot and VESAWeb",
  "version": "0.1.0",
  "author": "Jacob Heuman",
  "license": "LGPL-3.0-only",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "husky || true && npm run build",
    "prepack": "npm run build",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/",
    "lint:staged": "npx lint-staged",
    "bump-version": "./scripts/bump-version.sh"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.0.0",
    "eslint": "^9.12.0",
    "eslint-plugin-prettier": "^5.2.1",
    "husky": "^9.1.6",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.16.0"
  }
}
